# e282

**Stop your documentation examples from rotting.**

`e282` is a lightweight Dart tool that scans your source code for documentation comments, extracts the code blocks, and analyzes them to ensure they are not rotten.

## 📦 Usage

```bash
dart pub global activate e282
dart pub global run e282
```